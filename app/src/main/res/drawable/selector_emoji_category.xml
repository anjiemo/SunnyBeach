<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 按压状态 -->
    <item android:drawable="@drawable/bg_emoji_category_selected" android:state_pressed="true" />

    <!-- 选中状态 -->
    <item android:drawable="@drawable/bg_emoji_category_selected" android:state_selected="true" />

    <!-- 焦点状态 -->
    <item android:drawable="@drawable/bg_emoji_category_selected" android:state_focused="true" />

    <!-- 默认状态 -->
    <item android:drawable="@android:color/transparent" />

</selector>