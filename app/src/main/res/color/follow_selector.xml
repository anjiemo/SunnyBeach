<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 选中状态 -->
    <item android:color="@color/follow_btn_text_normal_color" android:state_selected="true" />

    <!-- 未选状态 -->
    <item android:color="@color/btn_text_disable_color" android:state_selected="false" />

    <!-- 默认状态-->
    <item android:color="@color/follow_btn_bg_normal_color" />

</selector>